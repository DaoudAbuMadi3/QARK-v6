<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>QARK v5 - Android Security Vulnerability Report</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Roboto', sans-serif;
      background: linear-gradient(135deg, #0f2027 0%, #203a43 50%, #2c5364 100%);
      color: #eceff1;
      padding: 20px;
      line-height: 1.8;
    }
    
    .container {
      max-width: 1400px;
      margin: 0 auto;
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      padding: 40px;
    }
    
    .header {
      text-align: center;
      margin-bottom: 50px;
      padding: 30px;
      background: linear-gradient(145deg, rgba(66, 165, 245, 0.1), rgba(144, 202, 249, 0.05));
      border-radius: 15px;
      border: 1px solid rgba(66, 165, 245, 0.2);
    }
    
    .logo {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 15px;
      margin-bottom: 20px;
    }
    
    .shield-icon {
      font-size: 3em;
      color: #42a5f5;
      animation: pulse 2s ease-in-out infinite;
    }
    
    @keyframes pulse {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.6; }
    }
    
    h1 {
      font-size: 3em;
      font-weight: 700;
      background: linear-gradient(45deg, #42a5f5, #90caf9);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      margin-bottom: 10px;
    }
    
    .subtitle {
      font-size: 1.2em;
      color: #b0bec5;
      margin-bottom: 20px;
    }
    
    .developers {
      font-size: 1em;
      color: #90a4ae;
      margin-top: 15px;
    }
    
    .developers span {
      color: #64b5f6;
      margin: 0 10px;
    }
    
    .summary {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-bottom: 40px;
    }
    
    .summary-card {
      background: linear-gradient(145deg, rgba(55, 71, 79, 0.5), rgba(38, 50, 56, 0.5));
      border-radius: 12px;
      padding: 25px;
      text-align: center;
      border: 1px solid rgba(255, 255, 255, 0.1);
      transition: transform 0.3s, box-shadow 0.3s;
    }
    
    .summary-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    }
    
    .summary-card.vulnerability {
      border-left: 4px solid #ef5350;
    }
    
    .summary-card.warning {
      border-left: 4px solid #ffa726;
    }
    
    .summary-card.info {
      border-left: 4px solid #42a5f5;
    }
    
    .summary-icon {
      font-size: 2.5em;
      margin-bottom: 15px;
    }
    
    .summary-card.vulnerability .summary-icon {
      color: #ef5350;
    }
    
    .summary-card.warning .summary-icon {
      color: #ffa726;
    }
    
    .summary-card.info .summary-icon {
      color: #42a5f5;
    }
    
    .summary-number {
      font-size: 3em;
      font-weight: 700;
      color: #ffffff;
      margin-bottom: 10px;
    }
    
    .summary-label {
      font-size: 1.1em;
      color: #b0bec5;
    }
    
    .section-title {
      font-size: 2em;
      font-weight: 600;
      color: #ffffff;
      margin: 40px 0 20px;
      padding-bottom: 15px;
      border-bottom: 3px solid rgba(66, 165, 245, 0.3);
    }
    
    .issues-container {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    
    .issue {
      background: linear-gradient(145deg, rgba(55, 71, 79, 0.6), rgba(38, 50, 56, 0.6));
      border-radius: 12px;
      padding: 30px;
      border-left: 5px solid;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      transition: transform 0.3s, box-shadow 0.3s;
    }
    
    .issue:hover {
      transform: translateX(5px);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
    }
    
    .issue.vulnerability {
      border-left-color: #ef5350;
    }
    
    .issue.warning {
      border-left-color: #ffa726;
    }
    
    .issue.info {
      border-left-color: #42a5f5;
    }
    
    .issue-header {
      display: flex;
      align-items: flex-start;
      justify-content: space-between;
      margin-bottom: 20px;
      flex-wrap: wrap;
      gap: 15px;
    }
    
    .issue-title-container {
      flex: 1;
      min-width: 250px;
    }
    
    .issue-title {
      font-size: 1.5em;
      font-weight: 600;
      color: #ffffff;
      margin-bottom: 10px;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    
    .issue.vulnerability .issue-title {
      color: #ef5350;
    }
    
    .issue.warning .issue-title {
      color: #ffa726;
    }
    
    .issue.info .issue-title {
      color: #42a5f5;
    }
    
    .severity-badge {
      display: inline-block;
      padding: 8px 16px;
      border-radius: 20px;
      font-size: 0.9em;
      font-weight: 600;
      text-transform: uppercase;
    }
    
    .severity-badge.vulnerability {
      background: rgba(239, 83, 80, 0.2);
      color: #ef5350;
      border: 1px solid #ef5350;
    }
    
    .severity-badge.warning {
      background: rgba(255, 167, 38, 0.2);
      color: #ffa726;
      border: 1px solid #ffa726;
    }
    
    .severity-badge.info {
      background: rgba(66, 165, 245, 0.2);
      color: #42a5f5;
      border: 1px solid #42a5f5;
    }
    
    .issue-description {
      font-size: 1.1em;
      color: #cfd8dc;
      margin-bottom: 20px;
      line-height: 1.8;
    }
    
    .issue-file {
      background: rgba(0, 0, 0, 0.3);
      border-radius: 8px;
      padding: 15px;
      margin-top: 15px;
      font-family: 'Courier New', monospace;
    }
    
    .issue-file-label {
      font-weight: 600;
      color: #90caf9;
      margin-bottom: 8px;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    
    .issue-file-path {
      color: #eceff1;
      word-break: break-all;
      font-size: 0.95em;
    }
    
    .issue-tags {
      margin-top: 15px;
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }
    
    .tag {
      background: rgba(66, 165, 245, 0.2);
      color: #90caf9;
      padding: 5px 12px;
      border-radius: 15px;
      font-size: 0.85em;
      border: 1px solid rgba(66, 165, 245, 0.3);
    }
    
    .footer {
      text-align: center;
      margin-top: 60px;
      padding-top: 30px;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      color: #90a4ae;
      font-size: 0.95em;
    }
    
    .footer-divider {
      width: 100px;
      height: 3px;
      background: linear-gradient(90deg, #42a5f5, #90caf9);
      margin: 20px auto;
      border-radius: 2px;
    }
    
    @media (max-width: 768px) {
      .container {
        padding: 20px;
      }
      
      h1 {
        font-size: 2em;
      }
      
      .summary {
        grid-template-columns: 1fr;
      }
      
      .issue-header {
        flex-direction: column;
      }
    }
    
    @media print {
      body {
        background: white;
        color: black;
      }
      
      .container {
        box-shadow: none;
      }
      
      .issue:hover {
        transform: none;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <div class="logo">
        <i class="fas fa-shield-alt shield-icon"></i>
        <h1>QARK v5</h1>
      </div>
      <div class="subtitle">Quick Android Review Kit</div>
      <div class="subtitle">Android Application Security Vulnerability Report</div>
      <div class="developers">
        <p>Developed by</p>
        <p><span>Daoud Abu Madi</span></p>
      </div>
    </div>
    
    <!-- Summary -->
    {% set vulnerability_count = issues | selectattr('severity.name', 'equalto', 'VULNERABILITY') | list | length %}
    {% set warning_count = issues | selectattr('severity.name', 'equalto', 'WARNING') | list | length %}
    {% set info_count = issues | selectattr('severity.name', 'equalto', 'INFO') | list | length %}
    
    <div class="summary">
      <div class="summary-card vulnerability">
        <div class="summary-icon">
          <i class="fas fa-exclamation-triangle"></i>
        </div>
        <div class="summary-number">{{ vulnerability_count }}</div>
        <div class="summary-label">Critical Vulnerabilities</div>
      </div>
      
      <div class="summary-card warning">
        <div class="summary-icon">
          <i class="fas fa-exclamation-circle"></i>
        </div>
        <div class="summary-number">{{ warning_count }}</div>
        <div class="summary-label">Warnings</div>
      </div>
      
      <div class="summary-card info">
        <div class="summary-icon">
          <i class="fas fa-info-circle"></i>
        </div>
        <div class="summary-number">{{ info_count }}</div>
        <div class="summary-label">Info</div>
      </div>
      
      <div class="summary-card">
        <div class="summary-icon" style="color: #4caf50;">
          <i class="fas fa-list-check"></i>
        </div>
        <div class="summary-number">{{ issues | length }}</div>
        <div class="summary-label">Total Issues</div>
      </div>
    </div>
    
    <!-- Issues by Severity -->
    {% if vulnerability_count > 0 %}
    <h2 class="section-title">
      <i class="fas fa-bug"></i> Critical Vulnerabilities ({{ vulnerability_count }})
    </h2>
    <div class="issues-container">
      {% for issue in issues %}
        {% if issue.severity.name == 'VULNERABILITY' %}
        <div class="issue vulnerability">
          <div class="issue-header">
            <div class="issue-title-container">
              <div class="issue-title">
                <i class="fas fa-times-circle"></i>
                {{ issue.name }}
              </div>
            </div>
            <span class="severity-badge vulnerability">{{ issue.severity.name }}</span>
          </div>
          
          <div class="issue-description">{{ issue.description | e }}</div>
          
          {% if issue.file_object %}
          <div class="issue-file">
            <div class="issue-file-label">
              <i class="fas fa-file-code"></i>
              <strong>Location:</strong>
            </div>
            <div class="issue-file-path">
              {{ issue.file_object }}{% if issue.line_number %}:{{ issue.line_number[0] }}:{{ issue.line_number[1] }}{% endif %}
            </div>
          </div>
          {% endif %}
          
          {% if issue.tags %}
          <div class="issue-tags">
            {% for tag in issue.tags %}
            <span class="tag">{{ tag }}</span>
            {% endfor %}
          </div>
          {% endif %}
        </div>
        {% endif %}
      {% endfor %}
    </div>
    {% endif %}
    
    {% if warning_count > 0 %}
    <h2 class="section-title">
      <i class="fas fa-exclamation-triangle"></i> Warnings ({{ warning_count }})
    </h2>
    <div class="issues-container">
      {% for issue in issues %}
        {% if issue.severity.name == 'WARNING' %}
        <div class="issue warning">
          <div class="issue-header">
            <div class="issue-title-container">
              <div class="issue-title">
                <i class="fas fa-exclamation-circle"></i>
                {{ issue.name }}
              </div>
            </div>
            <span class="severity-badge warning">{{ issue.severity.name }}</span>
          </div>
          
          <div class="issue-description">{{ issue.description | e }}</div>
          
          {% if issue.file_object %}
          <div class="issue-file">
            <div class="issue-file-label">
              <i class="fas fa-file-code"></i>
              <strong>Location:</strong>
            </div>
            <div class="issue-file-path">
              {{ issue.file_object }}{% if issue.line_number %}:{{ issue.line_number[0] }}:{{ issue.line_number[1] }}{% endif %}
            </div>
          </div>
          {% endif %}
          
          {% if issue.tags %}
          <div class="issue-tags">
            {% for tag in issue.tags %}
            <span class="tag">{{ tag }}</span>
            {% endfor %}
          </div>
          {% endif %}
        </div>
        {% endif %}
      {% endfor %}
    </div>
    {% endif %}
    
    {% if info_count > 0 %}
    <h2 class="section-title">
      <i class="fas fa-info-circle"></i> Additional Information ({{ info_count }})
    </h2>
    <div class="issues-container">
      {% for issue in issues %}
        {% if issue.severity.name == 'INFO' %}
        <div class="issue info">
          <div class="issue-header">
            <div class="issue-title-container">
              <div class="issue-title">
                <i class="fas fa-info-circle"></i>
                {{ issue.name }}
              </div>
            </div>
            <span class="severity-badge info">{{ issue.severity.name }}</span>
          </div>
          
          <div class="issue-description">{{ issue.description | e }}</div>
          
          {% if issue.file_object %}
          <div class="issue-file">
            <div class="issue-file-label">
              <i class="fas fa-file-code"></i>
              <strong>Location:</strong>
            </div>
            <div class="issue-file-path">
              {{ issue.file_object }}{% if issue.line_number %}:{{ issue.line_number[0] }}:{{ issue.line_number[1] }}{% endif %}
            </div>
          </div>
          {% endif %}
          
          {% if issue.tags %}
          <div class="issue-tags">
            {% for tag in issue.tags %}
            <span class="tag">{{ tag }}</span>
            {% endfor %}
          </div>
          {% endif %}
        </div>
        {% endif %}
      {% endfor %}
    </div>
    {% endif %}
    
    <!-- Footer -->
    <div class="footer">
      <div class="footer-divider"></div>
      <p>Report generated by QARK v5</p>
      <p>Quick Android Review Kit</p>
      <p style="margin-top: 10px; font-size: 0.9em;">
        <i class="fas fa-calendar-alt"></i> 
        Report Date: {{ current_date }}
      </p>
    </div>
  </div>
</body>
</html>
